# Gemini API Key (required for NL intent parsing)
GEMINI_API_KEY="your-gemini-api-key"

# LLM model (optional, defaults to gemini-2.5-flash)
# LLM_MODEL="gemini-2.5-flash"

# Server port (optional, defaults to 3001)
# PORT=3001
